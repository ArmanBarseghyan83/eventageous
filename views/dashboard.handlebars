

<section >
    <a href="/dashboard/create" > <button type="submit" class="btn btn-primary mb-3">Create New Event</button></a>
    <div class="row">
        {{#each events}}
        {{> dashboardItem}}
        {{/each}}
    </div>
</section>